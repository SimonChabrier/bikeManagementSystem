{% extends 'base.html.twig' %}
{% block title %}Créer Inventaire{% endblock %}

{% block body %}
<div class="container"> 
    <h1>Créer un nouvel inventaire</h1>
    {# {{ include('inventory/_form.html.twig') }} #}
    {{ include('inventory/_inventory_api_post.html.twig') }}
      
      <a href="{{ path('app_inventory_index') }}">
        <button class="btn btn-lg btn-primary" type="submit">Retour à la liste</button>
      </a>

    <h5>Inventaires du jour</h5>
    {{ include('inventory/_today_inventory_list.html.twig') }}
</div>
{% endblock %}
