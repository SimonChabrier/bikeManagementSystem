{% extends 'base.html.twig' %}

{% block title %}Edit Inventory{% endblock %}

{% block body %}
<div class="container"> 
    <h1>Modifier l'inventaire - {{ inventory.id }}</h1>
    {{ include('inventory/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <a href="{{ path('app_inventory_index') }}"><button class="btn btn-lg btn-primary" type="submit">Retour liste inventaires</button></a>
    <hr>
    {{ include('inventory/_delete_form.html.twig') }}
</div>
{% endblock %}
