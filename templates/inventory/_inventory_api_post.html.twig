{% block javascripts %}
    <script src="{{ asset('assets/js/inventoryForm.js') }}" defer></script>
{% endblock %}

<form>
<div class="row">
    <div class="col">
        <label for="bikeSelect" class="mb-2" id="availableBikesCount">Vélos select</label>
        <select class="form-control" id="bikes">
            <option selected disabled>-- Choix Vélo --</option>
            {# here list options are generate via JS #}
        </select>
    </div>
    <div class="col">
        <label for="stationSelect" class="mb-2" id="availableStationsCount">Station select</label>
        <select class="form-control" id="stations" placeholder="choisir une stations">
            <option selected >-- Choix Station --</option>
            {# here list options are generate via JS #}
        </select>
    </div>
</div>  
<h5 id="bikesSelecTitle">Selection</h5>
<div id="selectedBikes" class="mt-3 mb-2">
</div>
  <button type="submit" id="formSubmitButton" class="btn btn-success mb-2">Valider</button>
</form>