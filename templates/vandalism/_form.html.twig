{{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.bike) }}
    {{ form_row(form.station) }}      
    {{ form_row(form.content) }}      
    {{ form_row(form.mainPicture) }}
    {{ form_row(form.status) }}
    <hr>
    <button class="btn btn-lg btn-success" type="submit">Enregistrer le vandalime</button>
{{ form_end(form) }}