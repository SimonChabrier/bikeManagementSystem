{% extends 'base.html.twig' %}
{% block title %}Admin Vandalismes{% endblock %}

{% block body %}

<div class="container">
    <h1>affichage des vandalismes</h1>
    <h5>liste l'ensemble des vandalismes constatés</h5>
    <ul>
    {% for vandalism in vandalisms %}
        <li class='my-3 py-1 px-1 py-2 text-white bg-secondary'>N° {{ vandalism.id }} - le {{ vandalism.createdAt|date("d/m/Y") }}</li>
        <li class='mt-3'> {{ vandalism.content | slice(0, 120)}} <a href="{{ path('app_vandalism_show', {'id': vandalism.id}) }}">...</a></li>
        {% if vandalism.mainPicture %}
        <hr>
        {# <img src = "{{ asset('uploads/main_pictures/' ~ vandalism.mainPicture) }}" style="width: 150px%; height: 150px; object-fit: cover" alt="photographie de la station montrant un vélo vandalisé"> #}
        <img src = "{{ vandalism.mainPicture }}" style="width: 150px%; height: 150px; object-fit: cover" alt="photographie de la station montrant un vélo vandalisé">
        <hr>
        {% endif %}
        <li class='mt-2'>
            <a href="{{ path('app_vandalism_show', {'id': vandalism.id}) }}">Afficher </a>
            <div class="vr"></div>
            <a href="{{ path('app_vandalism_edit', {'id': vandalism.id}) }}">  Modifier</a>
        </li>
         {% else %}
        <tr>
            <td colspan="19"><div class="alert alert-danger" role="alert">
            Pas d'enregistrement de vadalimes en Base de Données !
            </td>
        </tr>
    {% endfor %}
    </ul>
    <hr>
     <a href="{{ path('app_vandalism_new') }}"><button class="btn btn-lg btn-primary" type="submit">Signaler un vandalime</button></a>
</div>
{% endblock %}


{# {% extends 'base.html.twig' %}

{% block title %}Vandalism index{% endblock %}

{% block body %}
    <h1>Vandalism index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Content</th>
                <th>Status</th>
                <th>CreatedAt</th>
                <th>MainPicture</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vandalism in vandalisms %}
            <tr>
                <td>{{ vandalism.id }}</td>
                <td>{{ vandalism.content }}</td>
                <td>{{ vandalism.status ? 'Yes' : 'No' }}</td>
                <td>{{ vandalism.createdAt ? vandalism.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ vandalism.mainPicture }}</td>
                <td>
                    <a href="{{ path('app_vandalism_show', {'id': vandalism.id}) }}">show</a>
                    <a href="{{ path('app_vandalism_edit', {'id': vandalism.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_vandalism_new') }}">Create new</a>
{% endblock %} #}
