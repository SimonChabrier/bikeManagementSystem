<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="{{ path('app_home') }}">Accueil</a>  {# Bouton home à styliser #}
    <!-- burger button -->
    <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>  
    <div class="collapse navbar-collapse collapsed" id="navbarSupportedContent"><!-- nav content start -->   
      <ul class="navbar-nav me-auto mb-2 mb-lg-0"><!-- nav items start --> 
         {# {% if app.user %}<li><a class="dropdown-item" href="{{ path('list_station') }}">Tournée</a></li>{% endif %}
         {% if app.user %}<li><a class="dropdown-item" href="{{ path('list_station') }}">Interventions</a></li>{% endif %}
         {% if app.user %}<li><a class="dropdown-item" href="{{ path('list_station') }}">Tableau de bord</a></li>{% endif %}
         {% if app.user %}<li><a class="dropdown-item" href="{{ path('list_station') }}">Vélos</a></li>{% endif %}
         {% if app.user %}<li><a class="dropdown-item" href="{{ path('list_station') }}">Stations</a></li>{% endif %}
         {% if app.user %}<li><a class="dropdown-item" href="{{ path('list_station') }}">Pannes</a></li>{% endif %} #}
       
       
        {# <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Utilisateur
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            {% if app.user %}
            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
            <li><hr class="dropdown-divider"></li>
            {% else %}
            <li><a class="dropdown-item" href="{{ path('app_login') }}">Identification</a></li>
            {% endif %}

            {% if app.user %}
            {% else %}
            <li><a class="dropdown-item" href="{{ path('app_register') }}">Créer un compte utilisateur</a></li>
            <li><hr class="dropdown-divider"></li>
            {% endif %}

            {% if app.user %}
            <li><a class="dropdown-item" href="{{ path('app_forgot_password_request') }}">Changer de mot de passe !</a></li>
            {% else %}
            <li><a class="dropdown-item" href="{{ path('app_forgot_password_request') }}">Mot de passe oublié !</a></li>
            {% endif %}
          </ul>
          <!-- Pages -->
          {% if app.user %}
          <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Pages
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
         
          <li><hr class="dropdown-divider"></li>
          </ul>
          {% endif %}
          <!-- End Pages -->      
        </li> #}

        
      </ul><!-- nav items end --> 
    </div><!-- nav content end --> 
  </div><!-- container end -->
</nav>

{# add a link source code :
<li class="nav-item">
<a class="nav-link" href="#">Add a Link</a>
</li>
#}