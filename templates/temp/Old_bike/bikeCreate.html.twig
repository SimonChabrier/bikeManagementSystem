{% extends 'base.html.twig' %}

{% block title %}Bike Create{% endblock %}

{% block body %}


<div class="container">
<h1>EDITION VELO INFOS</h1>
</div>

<div class="container">
{{ form_start(form, {attr: {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.status) }}
    {{ form_row(form.reference) }}
    {{ form_row(form.number) }}
    {{ form_row(form.rate) }}      
    <hr>
    <button class="btn btn-lg btn-success" type="submit">Enregistrer le vélo</button>
{{ form_end(form) }}
</div>
{% endblock %}
