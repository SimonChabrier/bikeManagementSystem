{% extends 'base.html.twig' %}

{% block title %}Edition Equilibrage - N° {{ balance.id }}{% endblock %}

{% block body %}
<div class="container"> 
    <h1>Modifier l'équilibrage - N° {{ balance.id }}</h1>
    <h5>Du {{ balance.createdAt ? balance.createdAt|date('d-m-Y à H:i') : '' }}</h5>
    {{ include('balance/_form.html.twig', {'button_label': 'Update'}) }}
    <a href="{{ path('app_balance_index') }}"><button class="btn btn-lg btn-primary" type="submit">Retour liste équilibrages</button></a>
    <hr>
    {{ include('balance/_delete_form.html.twig') }}
{% endblock %}
