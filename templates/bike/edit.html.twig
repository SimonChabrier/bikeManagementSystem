{% extends 'base.html.twig' %}
{% block title %}Edition Vélo{% endblock %}

{% block body %}
<div class="container"> 
    <h1>Modifier ce vélo</h1>
    {{ include('bike/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="container">
    <button class="btn btn-lg btn-primary" type="submit"><a href="{{ path('app_bike_index') }}">Retour liste vélos</a></button>
    <hr>
    {{ include('bike/_delete_form.html.twig') }}
</div>
{% endblock %}
